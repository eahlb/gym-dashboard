extends layout 

block content 
  h1 Create workout
  form.small-text(action='/workout', method='POST')
    div
      label Workout name: 
      input.grow(type='text', name='workoutName', required='true')
    div 
      button.small-text(type='submit') Create workout
